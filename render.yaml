services:
  - type: web
    name: cappybeam-services-checks
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "pnl site v2:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.16
      - key: SECRET_KEY
        generateValue: true 